#include <stdio.h>

int main ()
{
    int *p;
    int x,y,z;
    //x=10;
    //y=20;
    p = &x;
   // q = &x;
    printf("%d",);
    printf("%d\n",*(&p));
    printf("%p\n",&(*p));
    
  //  *p = *p + 1;
   // *q = *q + 2;
   // z = *p/(*q);
    //printf("%d %d %d\n",*p,*q,z);
    return 0;
    
}