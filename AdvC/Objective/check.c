#include<stdio.h>
int main()
{
    int i = 0, j = 0;
    printf("%d\n", i++ + ++j);
//return 0;
// int a = 1, b = 0;
// if (++a || ++b)
// printf("In first if a = %d, b = %d\n", a, b);
// a = 1, b = 0;

// if (b++ && ++a)
// printf("if true then this In second if a = %d, b = %d\n", a, b);
// else
// printf("In second if a = %d, b = %d\n", a, b);
return 0;
}
