// Write a C program to print numbers starting from 1. As soon as you find the first number that is divisible by both 4 and 6, 
// display the number and terminate the loop using the break statement.
#include <stdio.h>

int main (){
    int i=1;
    while(1)
    {
        // if(i % 4 == 0 && i % 6 == 0)
        // {
        //     printf("%d\n",i);
        //     break;
            
        // }
        // i++;
        printf("%d")
    }
    return 0;
}