#include <stdio.h>
#include<string.h>


int main ()
{   

    char str[] = "HelloWorld";
    int length = strlen(str);
    printf("The length of the given string is %d\n",length);
    

    
    return 0;
}